apiVersion: v1
kind: Service
metadata:
  name: calcule-horaires-service
  namespace: horaires-app
  labels:
    app: calcule-horaires
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: calcule-horaires
